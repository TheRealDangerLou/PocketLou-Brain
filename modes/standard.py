"""STANDARD — Lou's default mode. Full profile, ready for anything."""


class StandardMode:
    name = "STANDARD"
    description = "Full Lou profile loaded, ready for anything"
    temperature = 0.75
    max_tokens = 1024
    prefer_local = True
    haptic_on_switch = "mode_switch"
